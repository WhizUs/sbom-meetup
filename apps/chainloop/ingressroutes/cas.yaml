---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: chainloop-cas-api
spec:
  tls: {}
  entryPoints:
    - websecure
  routes:
    - match: Host(`api-cas-chainloop.local.computer`) && PathPrefix(`/`)
      kind: Rule
      services:
        - name: chainloop-cas-api
          namespace: chainloop
          port: grpc
          scheme: h2c
          passHostHeader: true
        # - name: chainloop-cas
        #   namespace: chainloop
        #   port: http
